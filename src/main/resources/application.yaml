spring:
  application:
    name: plusfy
  profiles:
    active: local

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

server:
  port: 8081

app:
  jwt:
    expiration-ms: 86400000